App(
    appid="gpio",
    name="GPIO",
    apptype=FlipperAppType.EXTERNAL,
    entry_point="gpio_app",
    cdefines=["APP_GPIO"],
    requires=["gui"],
    stack_size=1 * 1024,
    # icon="A_GPIO_14",
    order=50,
    fap_icon="gpioIcon.png",
    fap_category="Main",
    fap_icon_assets="images",
    fap_libs=["assets"],
)
